{
  "name": "your-app-name",
  "version": "1.0.0",
  "scripts": {
    "start": "node app.js",
    "test": "jest",
    "test:ci": "jest --ci --reporters=default --reporters=jest-junit",
    "build": "echo 'Building...'"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0"
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}